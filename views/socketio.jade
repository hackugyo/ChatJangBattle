script(src='http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js')
script(src='/socket.io/socket.io.js')
script(type='text/javascript', src='/javascripts/client.js')

h1 JANG Battle!!
div
  h2 説明
  p 名前を入れたら手を入力してください．
  p 他のプレイヤとその手でじゃんけんをして，勝った数だけポイントが入ります．
  p 20点先取．
  p 他のプレイヤもリアルタイムで手を変えてくるので，見切って適切な手を出しましょう．
  p Name:
    input#name(type='text', id='name')
  form(onsubmit='return send_p()', style='float:left;')
    input(type='submit',value='ぱー', id='hand_p')
  form(onsubmit='return send_c()', style='float:left;')
    input(type='submit',value='ちょき', id='hand_c')
  form(onsubmit='return send_g()', style='float:left;')
    input(type='submit',value='ぐー', id='hand_g')
  p(style='clear: both;')
    
div
  h3 Log
  ul#log


div
  h3 test
  ul#test
    li テスト
    li テスト
    